#!/bin/sh 

# git -C ./proto pull
# make protobuf
# git add .
# git commit -m "$(shell git show --pretty=format:%s -s HEAD -C ./proto)"
# git push 
# PREVIOUS_TAG=$(git describe --tags --abbrev=0)
# echo $PREVIOUS_TAG
# n=${current_version##*[!0-9]}; p=${PREVIOUS_TAG%%$n}
# next_version=$p$((n+1))
# echo $next_version

